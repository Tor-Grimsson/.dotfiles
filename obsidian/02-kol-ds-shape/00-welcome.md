# 02 — Design-system shape (minimal)

Dummy note so this folder opens as a testable Obsidian vault. This is the **minimal** `.obsidian/` config source (core plugins only) — for lightweight doc trees like a design system's `docs/`.

Every repo symlinked to `02-kol-ds-shape/.obsidian` inherits changes made here. See `../INDEX.md`.
