---
name: project_glass_naming_system
description: "The three-family naming system for the agent-system export — Glass=state, Ubu Roi=actors, Alice=motion; pick from the bag, never invent individually. Must be documented in both .kol (agent) and docs/ (human) when the outline work starts."
metadata: 
  node_type: memory
  type: project
  originSessionId: b62a9d8f-57ff-4ef3-bc9a-e70b95abcbc4
  modified: 2026-07-28T08:47:28.244Z
---

Naming for the agent-system export is a lookup, not a decision (settled 2026-07-28). Three families own three domains:

- **Glass = state** (holds/shows context): `memory-glass` (built) · seeds: `hourglass` (journaling), `spyglass` (routing/search), `weather-glass` (status).
- **Ubu Roi = actors** (persona, behavior, hooks): Ubu Roi (published persona — never "Grim" in public material) · seeds: `ubu-enchainé` (goal-loop hook), `pataphysics` (rules spec), palotins (subagents).
- **Alice = motion** (init, traversal, sync, search journeys): seeds: `rabbit-hole` (boot/init), `cheshire` (session-bridge/handoff), `red-queen` (context trim), `white-rabbit` (cron), `jabberwock` (word-soup, slain by the clarity gate).

Umbrella: **`jabberwocky`** — assigned by the user 2026-07-28 (`~/dev/projects/kol-humpty-dumpty/jabberwocky`, public export). `humpty-dumpty` chambered unassigned. Bags are English-only (heavy French cut: enchainé, pataphysics). Orthography: compounds hyphenate, canonical single names one word. Documented: `.kol/llm-context/NAMING.md` + `docs/kol-agent-system/10-naming.md`. Related: [[feedback_user_names_are_binding]], [[feedback_clarity_over_cleverness]].
