---
name: output-l5
description: Render the NEXT reply as layout L5 — staged. Bold sub-heads and one blockquote for the single load-bearing warning. Triggered by /output-l5 (user-invoked only).
---

# /output-l5 — staged

Render **the next reply only** in layout **L5**.

**Modules:** 02 · lead · 03 · 04

**Fits:** a plan or walkthrough with distinct parts

## The definition lives in the register, not here

Read **`~/dev/projects/kol-dumpty/humpty/docs/documentation/08-formats/LAYOUT-REGISTER.md`** § L5 and follow it exactly. This file is a
pointer — if it and the register disagree, **the register wins**. A rendered example of this
layout, with real content: **`docs/documentation/08-formats/LAYOUT-GALLERY.md`** § L5. Do not restate the layout here;
two copies of a format is how a format drifts.

## Rules that bind every layout call

- **One reply.** It does not persist, it is not a mode. Standing shape is `/output`.
- **Never touch the dial.** `$humpty <n>`, `where`, `box` are the user's (ARCHITECTURE §3).
  A layout that needs a module he has switched off is not available — say so in one line, do not
  switch it on.
- **The payload still decides.** If the content does not fit L5, say which layout it fits and why
  in one line, then use the one he asked for anyway. He asked.
