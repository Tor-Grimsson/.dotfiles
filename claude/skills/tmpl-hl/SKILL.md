---
name: tmpl-hl
description: High-level summary only — the shape of the thing, not its detail. Then step through it together, flexibly, one piece at a time. No deep dives, no full plans, no execution. Triggered by /tmpl-hl (user-invoked only).
---

# tmpl-hl — high level, then we walk it

The user wants the **altitude**, not the terrain. Detail arrives on request, one step at a time.

## Shape

- **What it is** — one or two sentences.
- **The parts** — 3–6 named pieces, one line each. Names, not explanations.
- **Where it's rough** — one line, only if a piece is genuinely unresolved.

Then stop and wait. The next message picks a piece; you go one level deeper on *that piece only*.

## Rules

- No numbered execution plan (that's `tmpl-present`).
- No file lists, no line numbers, no code — unless the piece under discussion is the code.
- Don't pre-answer the follow-ups. Leaving the obvious question unanswered is correct here.
- If the user picks a piece, stay inside it; adjacent pieces wait their turn.
