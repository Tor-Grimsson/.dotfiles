# kol-packages

Catch-all for single skills that don't need their own top-level family folder. Share this one
`INDEX.md` — add a row per addition, don't create a new top-level group for one skill.

| Folder | Serves |
|---|---|
| `alga-tmpl/` | `algorithmic-art` skill — `generator_template.js` + `viewer.html`, the required starting point for p5.js artifacts |
