# kol-cdn

Backblaze B2 + Cloudflare R2 — one skill per provider, plus a pure-orientation sibling.

| Folder | Serves | Holds |
|---|---|---|
| `kol-bucket-b2/` | `kol-bucket-b2` skill | `kol-bucket-b2` — the CLI (symlinked as `bucket` in `~/.local/bin`) |
| `kol-bucket-r2/` | `kol-bucket-r2` skill | `kol-bucket-r2` — the CLI (symlinked as `bucket-r2` in `~/.local/bin`) |
| `kol-cdn-overview/` | `kol-cdn-overview` skill | just `INDEX.md` — no package, orientation-only, folder kept for symmetry |
