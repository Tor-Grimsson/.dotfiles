# 01 — Vault shape (rich)

Dummy note so this folder opens as a testable Obsidian vault. This is the **rich** `.obsidian/` config source (plugins, snippets, themes, hotkeys) — the default a full working vault symlinks.

Test plugins and tweak config here; every repo symlinked to `01-vault-shape/.obsidian` inherits the change. See `../INDEX.md`.
