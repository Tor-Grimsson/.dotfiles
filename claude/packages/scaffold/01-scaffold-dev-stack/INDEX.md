# scaffold-dev-stack

No package here. `scaffold-dev-stack` and `scaffold-dev-stack-kol` install from npm / generate
scaffolding directly in their own SKILL.md — nothing to depend on in `claude/packages/`. Folder
kept for symmetry with its two `scaffold/` siblings.
