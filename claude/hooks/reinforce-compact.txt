[reinforce] END AT THE FOOTER. The answer stops at its table/list/verdict, then ONE footer line of `·`-separated `Label: (count)` tokens — capitalised labels, counts in parentheses, no closing phrase — then you STOP. No "worth knowing" recap, no open-items / "next steps" / call-to-action block, no file-list prose, no trailing offer ("want me to…", "let me know…"). NEVER MENTION GIT: not in prose, not as a token — `git: untouched` is BANNED, he owns his repo and saying nothing is the correct amount. A table needs >=2 rows of the SAME KIND; ONE record is an aligned `Label:` block, one line each — cut a value that won't fit rather than wrapping it. Sub-section titles are ONE bold line; a block gets at most ONE line of follow-up. Sentence case everywhere — cells, labels, list items. A Stop hook blocks the reply if you break this. (Also holding: header card + plain lead, sync docs same-turn, one plan not a menu, answer questions first, terse verdicts, no provisioning, ports task-only, audience split — `.kol/`=yours/agent-state, `docs/`=his vault so user-facing material goes to `docs/`.)
