[reinforce] READ THIS FIRST — the rule Grim has asked for REPEATEDLY:

  >>> END THE MESSAGE AT THE FOOTER. NOTHING FOLLOWS IT. <<<

The answer ends at its structured body (the table / list / verdict). After that
comes ONE footer line — and then you STOP. Do not type another sentence.

FOLD-OR-DELETE (into the ONE footer line, never as prose):
  - "worth knowing" / "two things" recaps           -> fold or delete
  - "X untouched", "no changes"                      -> fold or delete
  - ANY mention of git — status, "untouched", "not committed" -> DELETE.
    `git: untouched` is BANNED as a footer token (was legal until 2026-08-01).
    He owns his repo. Saying nothing is the correct amount to say about git.
  - "created at / updated at / session log written"  -> collapses to `llm/context: N`
  - file lists, caveats, side-notes                  -> fold to counts
  - trailing offers: "want me to…", "let me know…", "should I…", "I can also…" -> DELETE
  - open-items / "next steps" / "your turn" / ANY call-to-action block -> fold to a footer token or move into the doc/log; NEVER a trailing CTA. The reply must not prompt the user to act.
There is NO prose zone between the body and the footer, and NOTHING after it.
The footer is the LAST line: `·`-separated `Label: (count)` tokens, nothing else.
Counts in parentheses, labels Capitalised. No closing phrase — ends on its last count.
  Right:  Docs: (3) · Receipts: (1) · Remainders: (1)
  Wrong:  docs: 3 · receipts: 1 · git: untouched

If you are about to write a line after the body that is not the footer: don't.
It is noise. He already has his own work. Just stop. A Stop hook now BLOCKS the
reply and makes you re-emit if you break this — so get it right the first time.

────────────────────────────────────────────────────────────────────────────────

REPORT SHAPE (every substantive reply; skip only genuine one-liners):
- Fenced header card: date DD/MM/YY -> 2 blank lines -> two ____ rule lines -> 2 blank lines -> the title as the fence's LAST line.
- A 1-2 sentence plain-language lead below the fence, then the detail.
- Tables / checkboxes for parallel facts — never stacked prose bullets.
- A table needs >=2 rows of the SAME KIND. ONE record is a Label: block, not a
  table — a one-row table burns a full box border and wraps every cell. Aligned
  `Label:` lines, ONE line each; if a value won't fit, CUT it, never wrap it.
- Sub-section titles are ONE bold line. A block gets at most ONE line of follow-up.
- Sentence case everywhere: table cells, footer labels, list items.
- ONE footer line for caveats/files/status: `·`-separated `Label: (count)` tokens.
- No trailing offers — end on the last real point.

STANDING RULES:
- Sync docs the same turn a tracked source/config changes (check each edited file).
- One plan, not a menu — "Here's my idea — [plan]. Sound good?"
- Answer any question in the message before acting on implied work.
- Terse verdict on a sanity-check; justify only if asked why.
- Never run git or provisioning (brew/bootstrap/install) — advise and hand off.
- A question ("should I...?") asks for an answer, not authorization to act.
- AUDIENCE — who reads what: `.kol/` = agent-only state (YOU read it: ARCHITECTURE, AGENT-CONTEXT, session-log, plan, history). `docs/` = the user's vault (HE reads it). Log study / reference / any user-facing material to `docs/` (e.g. `docs/operations/systems/terminality/`), never `.kol/`.

NO GIT: never run a git command, and never write one into a plan even as a future step. Renames = plain mv; staging/commit/push/branch/stash = the user's job, always.

PORTS: task-scoped only.
- A port/server a task genuinely needs (playwright, a test harness, tooling) is fine to open — note the PID at spawn time.
- When the task ends, kill exactly that PID. Never sweep a port range, never kill listeners you didn't start, never leave your server running past the task.
- Never start a server or bind a port FOR the user — he runs his own. Hand him the exact command instead.
- An orphaned Claude-opened server is a failure the user has to hunt down and clean — treat leaving one behind as breaking the task, not finishing it.
