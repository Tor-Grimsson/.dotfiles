# Handoff — 2026-06-14 19:19 — MCP (DEFERRED / CONDITIONAL)

> [!IMPORTANT] ACTIVATION GATE — read this first
> **This handoff is NOT the active task.** It is a dormant breadcrumb for *MCP work specifically*.
>
> If you (the agent reading this at init) were asked to do **MCP-related work** (glif / playwright / Gmail / Calendar / Drive servers, MCP auth, vault→env for an MCP token) — then this is for you; read on.
>
> If the current session is about **anything else** (yazi, shell, docs, scripts, tmux, …) — **ignore this handoff. It is passive history.** The startup protocol surfaced it only because it is timestamped newer than the latest session log; that does not make it your task. Fall back to `AGENT-CONTEXT.md` + the newest `session-log/` and ask the user what they want.
>
> The MCP arc has **not started** — nothing is mid-flight, nothing is broken right now. This note just gathers the scattered MCP threads into one place so whoever picks up MCP work doesn't have to reassemble them from the open-items list.

## Goal of the current arc
Get the project's MCP servers fully wired and authenticating without manual per-shell fuss — chiefly the **glif** MCP, whose token isn't reaching the environment yet.

## Last actions taken (causal trail, newest first)
- Nothing this session — this is a forward-looking placeholder created during the yazi session's `/log-work`, at the user's request, to gate the MCP work for a future session.
- (Historical, from prior sessions) glif + playwright were registered **user-scope** in `~/.claude.json` via `claude mcp add` — those lines are in `bootstrap.sh`. `settings.json`'s `mcpServers` block is **ignored** by Claude Code, which is why it was stripped earlier.

## Current state / open decision points
The MCP threads currently scattered across `AGENT-CONTEXT.md` "Open items", consolidated:
- **glif MCP fails auth** — it expects `GLIF_API_TOKEN` in the environment, which is not exported in any shell yet. The token lives in Bitwarden at **`kol-tokens/Glif`, in the NOTES field (not the password field)**.
- **Delivery mechanism is the open decision:** plain `export GLIF_API_TOKEN=…` in a local untracked `~/.secrets` (the pattern used for the iMac's `llm` key) **vs.** adopting **Bitwarden Secrets Manager (BWS)** — `bws run -- <cmd>` injects vault secrets as env vars with no plaintext file. BWS is the purpose-built fix and the AGENT-CONTEXT secrets item flags it as "the real fix for the GLIF_API_TOKEN shell-export hookup." Free tier covers it; judged overkill for 1–2 keys, worth it at ~4+.
- **The hard constraint (do not trip this):** never `export ANTHROPIC_API_KEY` on a machine running Claude Code on a subscription — it silently bills the API instead of the Max/Pro plan. Only set MCP/API tokens that a *non-Claude-Code* tool consumes. The MBP deliberately has no `~/.secrets`; glif isn't needed on the MBP.
- **Headless caveat:** interactively-authenticated MCP servers (claude.ai Gmail / Calendar / Drive) may be **absent in headless/cron runs** — don't build a routine that assumes they're present.

## Next intended action
When MCP work begins: decide `~/.secrets` export vs BWS, then wire `GLIF_API_TOKEN` from `kol-tokens/Glif` (NOTES field) so the glif MCP authenticates on the iMac; verify with a glif call. Keep the ANTHROPIC_API_KEY rule above in mind.

## Working memory not yet in AGENT-CONTEXT
- This handoff is itself the experiment in **conditional, init-gated handoffs**: a forward note that the startup protocol reads but that self-nullifies unless the session's task matches the gate. If this pattern proves useful, consider noting it in `session-bridge/README.md` as a handoff variant ("gated handoff").
- Never auto-deleted (per the bridge lifecycle); a future MCP session supersedes it with a real session log.
