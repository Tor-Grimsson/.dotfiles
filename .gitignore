.DS_Store
bin/tor-jackett
brewfile-mirror.txt
_tmp/
**/node_modules/
docs/.obsidian/

# Ephemeral /goal state (goal-loop Stop hook)
.kol/llm-context/.active-goal.md

# Secrets — never track these, even by accident (secrets live in Bitwarden / ~/.secrets)
.env
.env.*
.secrets
*.pem
*.key
*.p12
*.pfx
*_rsa
*_ed25519
*_ecdsa
.netrc
.pgpass
credentials
**/oauth

# kol-theme consumer symlinks (constant, machine-created by bin/kol-theme)
ghostty/current-theme.conf
kitty/current-theme.conf