-- linkarzu's nvim runs his own colors via neobean, which we don't carry —
-- the shelf's deep-blue tokyonight recolor is the closest stand-in until
-- a real linkarzu nvim port joins the shelf.
vim.cmd("colorscheme tokyonight")
