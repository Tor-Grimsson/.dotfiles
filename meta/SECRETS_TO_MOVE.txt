SECRETS INVENTORY — actual secrets for this setup, and where they live.
Convention: one Bitwarden Login item per secret, folder kol-tokens, key in the
password field. See BITWARDEN-SETUP.md (§5–6) for the store/load commands.

IN THE VAULT (kol-tokens) — confirmed 2026-06-05
- Jackett                  API key, in PASSWORD field (bin/tor-search reads it)
- Glif                     GLIF_API_TOKEN is in the NOTES field (password is empty!).
                           Read with: bw get notes Glif  (the other "Glif" item is the
                           kolkrabbi@kolkrabbi.io account login — different thing).
- rclone – kolkrabbi — b2  B2 creds, but with "account = "/"key = " prefixes baked into
                           the fields — strip them. See RCLONE-SETUP.md.
- Backblaze B2 Credentials B2 account MASTER key (separate from the rclone app key above).
- also present: kol-umami, Neon Postgresql, Stripe backup code, proton/bitwarden phrases.

DONE / NO LONGER PENDING
- MiniMax token   was leaked in vscode/settings.json → removed from repo. User: unused ~9
                  months, fine to leave dormant. ~/secrets-to-revoke.txt deleted.

VAULT HYGIENE (user's call — pre-existing duplicates, not touched)
- Two B2/rclone items overlap (Backblaze B2 Credentials vs rclone – kolkrabbi — b2) plus a
  3rd the agent created was DELETED 2026-06-05. Consider consolidating to one clean item
  with RAW values (no "key = " prefix).

NOT SECRETS / NOT TRACKED
- ~/.config/rclone/rclone.conf  machine-local; recreate from the vault (RCLONE-SETUP.md)
- ~/.ssh/ keys                  stay outside the repo, as always
