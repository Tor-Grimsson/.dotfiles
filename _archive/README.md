# _archive — superseded but referenceable

Tracked (NOT gitignored) parking for configs we replaced but still mine for features.
Unlike `~/_temp/` (machine-local junk), these stay in the repo on purpose.

| Dir | Was | Archived | Superseded by | Worth mining |
|---|---|---|---|---|
| `sketchybar/` | the menu bar (7 pure-shell widgets + apple power popup) | 2026-07-11 | Übersicht + simple-bar (`ubersicht/`) | `plugins/*.sh` data logic (cpu/mem/wifi/battery/weather/brew), `power.sh` popup, `colors.sh` accent palette |
