# Handoff — 2026-08-03 10:30

## Goal of the current arc

Get the humpty plugin off this iMac and installable anywhere. That is now done on the
iMac side: agent behaviour left `~/.dotfiles/claude/hooks/` entirely, the payload is
generated into a public repo, and the wiring works on a machine that has no dev checkout.

**The remaining half is the MBP: pull, restart, install, verify.** Read the section
addressed to it below before touching anything.

## Last actions taken (causal trail, newest first)

- `~/.claude/settings.local.json` written (untracked, machine-local) — overrides the
  marketplace back to `directory` **on this iMac only**, so hook edits stay live.
- `claude/settings.json` (shared) repointed to `github: Tor-Grimsson/ubu-roi`, and
  `statusLine` repointed to `bin/kol-statusline`.
- `bin/kol-statusline` written — a locator, because `$CLAUDE_PLUGIN_ROOT` is unset
  outside hooks and `statusLine` is not a hook.
- `ubu-roi` published. 67 files, generated by `humpty/bin/humpty-payload`.
- Skills cut 35 → 18 in humpty; the same 20 quarantined from dotfiles (82 → 62).
- 8 `output-l*` skills collapsed into one `layouts` skill with its register bundled.

---

## FOR THE MBP — do this, in this order

**1. Pull dotfiles.** It carries the new shared `settings.json` (github marketplace,
new statusLine) and `bin/kol-statusline`.

**2. Do NOT create `settings.local.json` there.** That file is the iMac's dev override.
The MBP should consume the published plugin, which is exactly what the shared settings
already say. Its absence is the correct state.

**3. Restart Claude Code, then install:**

```
/plugin install humpty@humpty
```

The marketplace is already registered in the shared settings — no `marketplace add` needed.

**4. Verify, in this order:**

| Check | Expect |
|---|---|
| Type `$humpty` | answers with the dial level |
| Statusline | the yellow `humpty-dumpty:strict` badge renders |
| `rm -rf` anything | **denied** by `humpty-rm` |
| `git status` | **denied** by `humpty-gate` (pipeline `off`) |

If the badge is blank but `$humpty` works, `bin/kol-statusline` found no plugin — check
`~/.claude/plugins/cache/` has a humpty version dir.

**5. Known MBP gap, unresolved:** `~/dev/projects` on that machine is a small subset with
no `kol-dumpty/` at all. Nothing in the new wiring needs it — the plugin comes from the
marketplace, and `kol-statusline` falls through to the cache when the dev repo is absent.
This is the first time that path is exercised, so it is the thing most likely to surprise.

---

## Current state / open decision points

- **The 18 kept skills are live in BOTH repos.** Plugin skills are namespaced —
  `humpty:layouts` and `humpty:humpty-goal` were confirmed in the live skill list this
  session. So the dotfiles copies are now shadowed duplicates. The user has not ruled on
  whether to quarantine them; do not decide it unasked.
- **8 concepts are specs, 0 packaged.** `concepts/09-memory` was never written.
  Deferred by the user, twice — not a gap to close unprompted.
- `docs/operations/systems/agent-system/12-setup-a-to-z.md` still describes the old hook
  wiring. Marked needs-a-pass, not corrected.
- **Framer agent — INSTALLED 2026-08-03**, after this handoff was first written.
  `npx @framer/agent setup` wrote two skills to `~/.claude/skills/`, which is the symlink
  into dotfiles — so `framer` and `framer-code-components` are now TRACKED (64 skills).
  Documented at `docs/documentation/04-dev-languages/13-framer-agent.md`. Verified: no
  credentials, `projects/` holds only `__template__`. **Vendored — never hand-edit**, the
  installer overwrites both dirs. Telemetry is ON by default; opt out with
  `npx @framer/agent telemetry disable`. **The MBP needs no install** — the skills arrive
  with the dotfiles pull.

## Next intended action

Install on the MBP per the numbered steps above, and report which of the four
verification rows pass. That is the first real test of the published plugin.

## Working memory not yet in AGENT-CONTEXT

- The layout collapse looked like tidying and was a **bug fix** — all eight `output-l*`
  skills pointed at a register in `docs/`, which does not ship. Same class as
  `humpty_stop.py` importing `format-check.py` from `docs/`. **When something in a skill
  points outside `skills/`, check whether the target ships.** That is the failure mode
  this whole arc kept producing.
- Running the payload's test suite *standalone* is what caught it: 11 asserts failed there
  that pass in the dev repo. "Tests green" in a repo carrying dev tooling says nothing
  about an install. `humpty-payload` now runs the suite as a gate.
- The delete gate blocked its own author clearing `__pycache__`. Bytecode caches are now
  scratch. Expect more of these — the gates are new and only live use finds the edges.
- Two of my findings this session were wrong before they were right: memory-glass
  "drift" (it was opt-in working correctly) and a `mode()` fixture "fix" that left the
  assert vacuous. Both were caught by testing rather than reading. Do that first.
